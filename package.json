{
    "name": "imageconverter",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "pnpm -r dev",
        "build": "pnpm -r build"
    },
    "devDependencies": {
        "concurrently": "^8.2.0"
    }
}